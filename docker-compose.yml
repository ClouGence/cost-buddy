services:
  cost-buddy:
    image: ${COST_BUDDY_IMAGE}
    container_name: cost-buddy
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "${COST_BUDDY_PORT}:8766"
    environment:
      SPRING_DATASOURCE_URL: ${SPRING_DATASOURCE_URL}
      SPRING_DATASOURCE_USERNAME: ${SPRING_DATASOURCE_USERNAME}
      SPRING_DATASOURCE_PASSWORD: ${SPRING_DATASOURCE_PASSWORD}
      MOTHERBOARD_ENABLED: ${MOTHERBOARD_ENABLED:-false}
      MOTHERBOARD_BASE_URL: ${MOTHERBOARD_BASE_URL:-}
      MOTHERBOARD_PRODUCT_ID: ${MOTHERBOARD_PRODUCT_ID:-0}
      MOTHERBOARD_FREE_PLAN_ID: ${MOTHERBOARD_FREE_PLAN_ID:-0}
      MOTHERBOARD_PRIVATE_KEY: ${MOTHERBOARD_PRIVATE_KEY:-}
      MOTHERBOARD_TIMEOUT: ${MOTHERBOARD_TIMEOUT:-30s}
      COST_BUDDY_SESSION_TIMEOUT: ${COST_BUDDY_SESSION_TIMEOUT:-7d}
      COST_BUDDY_SESSION_COOKIE_SECURE: ${COST_BUDDY_SESSION_COOKIE_SECURE:-false}
      COST_BUDDY_AUTH_SUCCESS_REDIRECT: ${COST_BUDDY_AUTH_SUCCESS_REDIRECT:-/}
      COST_BUDDY_AUTH_FAILURE_REDIRECT: ${COST_BUDDY_AUTH_FAILURE_REDIRECT:-/}
      COST_BUDDY_AUTH_GOOGLE_ENABLED: ${COST_BUDDY_AUTH_GOOGLE_ENABLED:-false}
      COST_BUDDY_AUTH_GOOGLE_CLIENT_ID: ${COST_BUDDY_AUTH_GOOGLE_CLIENT_ID:-}
      COST_BUDDY_AUTH_GOOGLE_REDIRECT_URI: ${COST_BUDDY_AUTH_GOOGLE_REDIRECT_URI:-}
      COST_BUDDY_AUTH_WECHAT_ENABLED: ${COST_BUDDY_AUTH_WECHAT_ENABLED:-false}
      COST_BUDDY_AUTH_WECHAT_APP_ID: ${COST_BUDDY_AUTH_WECHAT_APP_ID:-}
      COST_BUDDY_AUTH_WECHAT_REDIRECT_URI: ${COST_BUDDY_AUTH_WECHAT_REDIRECT_URI:-}
      LOGGING_FILE_NAME: /home/costbuddy/app/logs/cost-buddy.log
      JAVA_OPTS: ${JAVA_OPTS}
      TZ: Asia/Shanghai
    volumes:
      - ${COST_BUDDY_LOG_DIR}:/home/costbuddy/app/logs
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://127.0.0.1:8766/actuator/health | grep -q '\"status\":\"UP\"'"]
      interval: 10s
      timeout: 5s
      retries: 12
